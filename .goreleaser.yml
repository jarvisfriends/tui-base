version: 2

project_name: tui-base

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - id: tui-base
    binary: tui-base
    main: ./cmd/tui-base
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/jarvisfriends/tui-base/common.appVersion={{.Version}}

archives:
  - id: default
    formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ .Tag }}-next"

release:
  github:
    owner: jarvisfriends
    name: tui-base

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
